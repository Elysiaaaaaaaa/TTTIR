### must be same as
name: test
suffix: ~  # add suffix to saved images
model: ll_dataset
distortion: sr
scale: 1
crop_border: ~  # crop border when evaluation. If None(~), crop the scale pixels

datasets:
  test:  # the 1st test dataset
    name: LOLv2_r
    dataroot_GT: /data2/zhengkaihang/ttt/dataset/LOLv2/Real_captured/Test/Normal
    dataroot_LQ: /data2/zhengkaihang/ttt/dataset/LOLv2/Real_captured/Test/Low

#### network structures
network_G:
  which_model_G: TTTIR_enhance
  num_block: [1,1,1,1,1]
#### path
path:
  root: ./
  pretrain_model_G: pth/lolv1.pth
