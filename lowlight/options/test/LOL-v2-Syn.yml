### must be same as
name: lolv2_s
suffix: ~  # add suffix to saved images
model: ll_dataset
distortion: sr
scale: 1
crop_border: ~  # crop border when evaluation. If None(~), crop the scale pixels
gpu_ids: [0]

datasets:
  test:  # the 1st test dataset
    name: test
    dataroot_GT: /data2/zhengkaihang/ttt/dataset/LOLv2/Synthetic/Test/Normal
    dataroot_LQ: /data2/zhengkaihang/ttt/dataset/LOLv2/Synthetic/Test/low1

#### network structures
network_G:
  which_model_G: TTTIR
  num_block: [1,1,1,1,1]

#### path
path:
  root: ./
  pretrain_model_G: 
  